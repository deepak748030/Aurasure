// Native (iOS / Android) map: the real react-native-maps.
import MapViewDefault, { Marker } from 'react-native-maps';
import type { Region } from 'react-native-maps';

export { MapViewDefault as MapView, Marker };
export type { Region };
